<h1>Blog Posts</h1>
<%-# There's no equal sign in the statement below as there is no output here. We use equal sign only when we're outputting something -%>

<% @posts.each do |post| %>
    <div class="well">
        <h3><%= post.title %></h3>
        <p><%= post.body %></p>
        <%= link_to "Read More", post_path(post), :class => 'btn btn-default' %>
    </div>
<% end %>